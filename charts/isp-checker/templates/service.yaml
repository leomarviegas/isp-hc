apiVersion: v1
kind: Service
metadata:
  name: {{ include "isp-checker.fullname" . }}
  labels:
    {{- include "isp-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isp-checker.selectorLabels" . | nindent 4 }}